DROP TABLE LGONZALES.TEMP_CORRELATIVO CASCADE CONSTRAINTS;

CREATE GLOBAL TEMPORARY TABLE LGONZALES.TEMP_CORRELATIVO
(
  CDCC_COMPANIA    VARCHAR2(4 BYTE),
  NDCC_SERIE       VARCHAR2(4 BYTE),
  CDCC_TIPODOC     VARCHAR2(4 BYTE),
  NDCC_PREIMPRESO  VARCHAR2(40 BYTE)
)
ON COMMIT PRESERVE ROWS
NOCACHE;
